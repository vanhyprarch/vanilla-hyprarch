# Phase 2B test only; these are not production timeout defaults.
listener {
    timeout = 10
    on-timeout = vanhyprarch-screensaver start
}

listener {
    timeout = 9
    ignore_inhibit = true
    on-timeout = /usr/bin/true
    on-resume = vanhyprarch-screensaver stop
}
